// ====================== DONATE.JS ======================

function initDonate() {
  // Có thể mở rộng sau nếu cần load từ Firebase
  console.log(
    "%c💕 Donate section initialized",
    "color: #ff9edb; font-weight: bold;",
  );
}

window.initDonate = initDonate;
