document.addEventListener("keydown", function (event) {
  event.preventDefault(); // Prevent all key inputs
});

document.addEventListener("keypress", function (event) {
  event.preventDefault(); // Prevent all character inputs
});
